// AUTOGENERATED by manta-codegen. You MAY edit this file as a starting
// point for your own customizations — re-running codegen will overwrite.

#pragma once


#include <cstdio>
#include <string>

#include "ex1_craft.hpp"

struct Ex1CraftTelemetry {
    double t_sec = 0.0;
    // Top-level kinematic state (always populated).
    float p[3] = {0,0,0};
    float q[4] = {1,0,0,0};   // (w, x, y, z)
    float v[3] = {0,0,0};
    float w[3] = {0,0,0};
    struct Tx_zpT {
        float throttle{};
    } tx_zp;
    struct Tx_znT {
        float throttle{};
    } tx_zn;
    struct Ty_xpT {
        float throttle{};
    } ty_xp;
    struct Ty_xnT {
        float throttle{};
    } ty_xn;
    struct Tz_ypT {
        float throttle{};
    } tz_yp;
    struct Tz_ynT {
        float throttle{};
    } tz_yn;

    std::string to_json() const;
};

void capture_ex1_telemetry(const Ex1Craft& craft, double t_sec, Ex1CraftTelemetry& telem);

inline std::string Ex1CraftTelemetry::to_json() const {
    char buf[256];
    std::string s = "{";
    std::snprintf(buf, sizeof(buf),
        "\"t\":%.6f,\"p\":[%.6f,%.6f,%.6f],\"q\":[%.6f,%.6f,%.6f,%.6f],"
        "\"v\":[%.6f,%.6f,%.6f],\"w\":[%.6f,%.6f,%.6f]",
        t_sec,
        (double)p[0], (double)p[1], (double)p[2],
        (double)q[0], (double)q[1], (double)q[2], (double)q[3],
        (double)v[0], (double)v[1], (double)v[2],
        (double)w[0], (double)w[1], (double)w[2]);
    s += buf;
    s += ",\"tx_zp\":{";
    { char b[64]; std::snprintf(b, sizeof(b), "\"throttle\":%.6f", (double)tx_zp.throttle); s += b; }
    s += "}";
    s += ",\"tx_zn\":{";
    { char b[64]; std::snprintf(b, sizeof(b), "\"throttle\":%.6f", (double)tx_zn.throttle); s += b; }
    s += "}";
    s += ",\"ty_xp\":{";
    { char b[64]; std::snprintf(b, sizeof(b), "\"throttle\":%.6f", (double)ty_xp.throttle); s += b; }
    s += "}";
    s += ",\"ty_xn\":{";
    { char b[64]; std::snprintf(b, sizeof(b), "\"throttle\":%.6f", (double)ty_xn.throttle); s += b; }
    s += "}";
    s += ",\"tz_yp\":{";
    { char b[64]; std::snprintf(b, sizeof(b), "\"throttle\":%.6f", (double)tz_yp.throttle); s += b; }
    s += "}";
    s += ",\"tz_yn\":{";
    { char b[64]; std::snprintf(b, sizeof(b), "\"throttle\":%.6f", (double)tz_yn.throttle); s += b; }
    s += "}";
    s += "}";
    return s;
}

inline void capture_ex1_telemetry(const Ex1Craft& craft, double t_sec, Ex1CraftTelemetry& telem) {
    telem.t_sec = t_sec;
    auto p = craft.scene_to_craft().position();
    auto q = craft.scene_to_craft().orientation().raw();
    auto v = craft.scene_to_craft().vel_linear();
    auto w = craft.scene_to_craft().vel_angular();
    telem.p[0]=p.x(); telem.p[1]=p.y(); telem.p[2]=p.z();
    telem.q[0]=q.w(); telem.q[1]=q.x(); telem.q[2]=q.y(); telem.q[3]=q.z();
    telem.v[0]=v.x(); telem.v[1]=v.y(); telem.v[2]=v.z();
    telem.w[0]=w.x(); telem.w[1]=w.y(); telem.w[2]=w.z();
    telem.tx_zp.throttle = craft.tx_zp().throttle();
    telem.tx_zn.throttle = craft.tx_zn().throttle();
    telem.ty_xp.throttle = craft.ty_xp().throttle();
    telem.ty_xn.throttle = craft.ty_xn().throttle();
    telem.tz_yp.throttle = craft.tz_yp().throttle();
    telem.tz_yn.throttle = craft.tz_yn().throttle();
}
