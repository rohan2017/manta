// AUTOGENERATED by manta-codegen. You MAY edit this file as a starting
// point for your own customizations — re-running codegen will overwrite.

#pragma once

#include "ex8_sim.hpp"
#include "ex8_est.hpp"

namespace manta_gen::ex8 {

// Sim-tick parameters, frozen at codegen time. Drives the pacer
// in <target>_main.cpp; both sub-harnesses read DT through their
// own namespaces.
inline constexpr float DT             = 0.001f;
inline constexpr float SIM_RATE_MULT  = 1.0f;

// Composed setup: calls manta_gen::ex8_sim::setup() then
// manta_gen::ex8_est::setup().
void setup();

// Composed tick: runs the sim world, then the cross-world
// connect() steps, then the filter. Sub-harness ticks handle
// their own bindings and intra-world connects.
void tick();

// Composed shutdown: tears down the filter then the sim, in
// reverse-init order, before main() returns.
void shutdown();

}  // namespace manta_gen::ex8
