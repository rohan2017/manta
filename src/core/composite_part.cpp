// CompositePart is now a templated, header-only class (see
// include/manta/core/composite_part.hpp). This translation unit is kept
// empty so existing CMakeLists references resolve; remove from the build
// list when convenient.

#include "manta/core/composite_part.hpp"
