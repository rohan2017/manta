// PartT methods that need cross-class definitions (PartT::craft,
// PartT::field_ptr) are now defined inline at the bottom of
// include/manta/core/craft.hpp where both PartT and CraftT are available.
// This TU is empty (kept for build compat).

#include "manta/core/part.hpp"
